# Project Database Setup

## MongoDB Setup Instructions

1. Connect to MongoDB instance (I used mongosh off of MongoDB Atlas)
2. copy + paste `databaseSetup.js` file to `mongosh` to initialize the collections and indexes.
